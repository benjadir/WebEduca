import { CustomFabarsDirective } from './custom-fabars.directive';

describe('CustomFabarsDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFabarsDirective();
    expect(directive).toBeTruthy();
  });
});
